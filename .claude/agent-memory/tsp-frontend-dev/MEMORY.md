# Memory Index

- [rxResource API shape](feedback_rxresource_api.md) — use `params`/`stream`, not `request`/`loader` — TS2769 overload error otherwise
- [Vitest async DOM pattern](feedback_vitest_async_dom.md) — rxResource stubs need `await whenStable()` + second `detectChanges` before DOM assertions
